import 'package:flutter/material.dart';

class AdoptFinalScreen extends StatelessWidget {
  const AdoptFinalScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Adotar final'),
      ),
    );
  }
}
